# Enable 64-bit kernel
arm_64bit=1

# Kernel filename
kernel=kernel8.img

# Enable UART (Defaults to Debug UART on JST for Pi 5)
enable_uart=1

# Enable UART0 on GPIO 14/15 (The missing piece for Header UART)
dtoverlay=uart0

# Enable PCIe (Required for RP1 access)
dtparam=pciex1